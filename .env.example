# Configuración de Aplicación Planta Externa

PORT=3113
SESSION_SECRET=elin_pex_super_secret_session_key_2026

# Conexión PostgreSQL (Dentro de la red Docker o local)
DB_HOST=localhost
DB_PORT=5435
DB_USER=postgres
DB_PASSWORD=OlinPexPostgresPass2026!
DB_NAME=planta_externa

# Groq API (Para extracción de texto a JSON)
GROQ_API_KEY=tu_groq_api_key_aqui

# Respaldo de Firebase (Configuración del cliente Firebase para escribir a Firestore)
# Si deseas usar Firebase Admin SDK en el servidor, pon la ruta del archivo de credenciales JSON aquí:
FIREBASE_CREDENTIALS_PATH=
FIREBASE_PROJECT_ID=calculadora-olin
